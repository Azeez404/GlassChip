# Environment for the GLASSCHIP paper-analysis reproducibility runner
# (run_all.py -> validate + regenerate tables/figures/manifest + verify).
# This layer reads the FROZEN Phase 2A-2E result JSONs; it does NOT rerun the
# experiments and does NOT need the raw dataset.
#
# Python 3.13 (developed on 3.13.x). Versions are lower bounds.

numpy>=1.24
matplotlib>=3.7
# (standard library only otherwise: json, hashlib, pathlib, dataclasses, csv, re)

# --------------------------------------------------------------------------
# NOT required by this runner. Listed for completeness: the FROZEN Phase 2
# experiment scripts under v2_research/summit/ (never rerun here) used
#   polars>=1.4      # cleaned-data loading / ablation / bootstrap / fleet
#   scikit-learn>=1.9   # linear + HistGradientBoosting residual baselines
#   torch>=2.0 (CPU)    # small LSTM / physics-anchored MLP residual baselines
#   pyarrow>=14.0       # Parquet metadata
# xgboost is intentionally ABSENT: a scikit-learn HistGradientBoostingRegressor
# was used as the gradient-boosted-tree baseline (documented in the manuscript).
